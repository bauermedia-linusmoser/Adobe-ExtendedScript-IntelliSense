// Based on the tsconfig.json from the official Adobe CEP Ressources
// https://github.com/Adobe-CEP/Samples/blob/master/TypeScript/PProPanel-vscode/dom_app/tsconfig.json
{
    "compilerOptions": {
        "allowJs": true,
        "outDir":"./build", //prevent "cannot write file" error for CSInterface.js, due to allowJs
        "lib": ["es5"],
        "strict": true,
        "target": "es3"
    }
}